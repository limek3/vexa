# Calendar merged blocked slots

Что изменено:

- Заблокированные соседние слоты теперь сохраняются как единый интервал: `09:00–12:00`, а не набор `09:00–10:00`, `10:00–11:00`, `11:00–12:00`.
- В календаре такие интервалы отображаются одним цельным вертикальным блоком `Недоступно`.
- Один клик по цельному блоку разблокирует весь диапазон.
- Внутренние свободные карточки внутри заблокированного диапазона больше не рисуются.
- Визуал заблокированного диапазона приведён к виду общего состояния `Недоступно`: мягкий violet/purple паттерн, замок, общий диапазон времени.

Изменён файл:

- `app/dashboard/today/page.tsx`
