# ClickBook landing menu workspace pass

Что сделано:

- Взял визуальную модель меню из лендинга: фиксированный верхний bar, hover/click mega-menu, затемнение фона, blur, spring-like появление, intro-колонка, группы ссылок и promo-карточки.
- Перенёс меню в общий `WorkspaceShell`, поэтому оно применяется на всех dashboard-страницах.
- Desktop sidebar отключён. Мобильное меню оставлено прежним, чтобы не ломать адаптив.
- Mega-menu разбито под продукт КликБук:
  - Рабочее пространство
  - Запись и витрина
  - Клиенты
  - Управление
- Ширина topbar и mega-menu теперь завязана на общий `--cb-shell-width` и совпадает с шириной контента страниц.
- Контент страниц расширен: `--page-max-width` и `--cb-platform-max-width` подняты до `1480px`.
- Убраны старые desktop-отступы под левый sidebar.
- Сохранены события/уведомления, demo/live режим, публичная витрина и переход к созданию записи.

Изменённые файлы:

- `components/shared/workspace-shell.tsx`
- `app/globals.css`
