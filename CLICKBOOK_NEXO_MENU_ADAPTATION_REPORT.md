# ClickBook Nexo-style menu adaptation

Изменён файл:

- `components/shared/workspace-shell.tsx`

Что сделано:

1. Верхнее desktop-меню адаптировано под механику Nexo: фиксированная glass/pill-панель, горизонтальные смысловые разделы, большой mega-menu, затемнение фона при открытии.
2. Разделы переписаны под ClickBook:
   - `Для мастеров`
   - `Онлайн-запись`
   - `Клиенты и рост`
   - `Платформа`
3. Внутри каждого mega-menu есть:
   - intro-блок слева;
   - CTA;
   - ключевой поясняющий блок;
   - 2 колонки ссылок;
   - 2 промо-карточки снизу.
4. Меню работает на RU/EN, включая подписи CTA и правых кнопок.
5. Desktop sidebar не возвращался, mobile-навигация сохранена безопасно.
6. Активное состояние верхних пунктов сделано аккуратной нижней линией вместо тяжёлых прямоугольников.
7. Ссылки оставлены на существующие роуты проекта, включая demo-параметр.
8. Синтаксическая проверка `typescript.transpileModule` для изменённого файла проходит.

Полный `next build` не запускался: в архиве нет `node_modules`.
