# CLICKBOOK_SERVICES_CLIENTS_POLISH_FIX

## Что исправлено

- Устранён runtime/hydration error: `<button>` больше не является потомком другого `<button>` на странице `Услуги`.
- Строка услуги теперь кликабельная через `div role="button"` с клавиатурной поддержкой `Enter`/`Space`, а внутренние кнопки статуса остаются валидными интерактивными элементами.
- Убраны случайные цветовые заливки выбранной услуги от категории/цвета услуги. Активная строка теперь подсвечивается нейтрально через общий accent проекта.
- Приведены badges/status-кнопки к читаемой палитре в dark и light theme.
- Убран лишний пятый экспортный виджет в верхних метриках клиентов.
- Для светлой темы добавлены корректные divider/border слои, нейтральные поля, карточки и toggle-состояния.
- Дополнительно проверены страницы `Клиенты` и `Услуги` через `esbuild`.

## Изменённые файлы

- `app/dashboard/services/page.tsx`
- `app/dashboard/clients/page.tsx`
