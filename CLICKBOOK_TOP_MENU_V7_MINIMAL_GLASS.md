# ClickBook top menu v7

Переделано меню без внутренних квадратных подложек.

Изменено:
- `components/shared/workspace-shell.tsx`
- `app/globals.css`

Что сделано:
- новый спокойный glass topbar;
- основное меню без прямоугольников внутри, только текст, тонкие иконки и плавная нижняя линия;
- dropdown языка и темы используют тот же glass-surface, что и mega-menu;
- пункты в dropdown без залитых карточек: активность через тонкую вертикальную линию;
- mega-menu очищен от тяжелых внутренних плиток, hover тоже через линию;
- убраны визуальные конфликты `язык/тема` vs большой dropdown.

Полноценную сборку не запускал: в архиве нет `node_modules`, поэтому `eslint` недоступен. `tsc --noEmit` тоже упирается в отсутствующие типы React/Next из зависимостей.
