# /desktop menu color fix

Исправление для тёмной темы /desktop: левое меню теперь использует тот же цвет, что и Electron-оболочка/titlebar — `#101820`.

Заменяемый файл:

- `app/desktop/desktop.css`

Применение: распаковать архив в корень проекта с заменой файлов.
